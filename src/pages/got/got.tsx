import { Characters } from '../../got/components/characters/characters';
import './styles.css';

function Got() {
  return (
    <section className="got">
      <Characters></Characters>
    </section>
  );
}

export default Got;
